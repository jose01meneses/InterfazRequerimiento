import AddForm from "../../../addForm";
//verificacion de comentario en git
const revenue = () => {
  const title = "Revenue";
  return <AddForm componentsView={title} />;
};

export default revenue;
